{
  "name": "@kongulo-is/euneo",
  "version": "0.0.13",
  "description": "Euneo types",
  "repository": "kongulo-is/euneo-types",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "build": "tsc",
    "patch": "tsc && npm version patch && npm publish",
    "pre": "rm ./lib/* && tsc && npm version patch"
  },
  "files": [
    "lib"
  ],
  "dependencies": {
    "@firebase/firestore": "^4.1.3",
    "@firebase/firestore-types": "^3.0.0",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.5.9",
    "typescript": "^5.2.2"
  },
  "exports": {
    ".": "./lib/index.js"
  }
}
